<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Blog Webapp</title>
        <link rel="stylesheet" href="stylesheets/style.css">
        <script src="https://cdn.tailwindcss.com"></script>
    </head>

    <body>
        <div class="w-full h-full p-10 text-white">
            <div class="mb-4 text-blue-600 mb-3">
                <h3><a href="/create">Back to Home</a></h3>
            </div>
            <h1 class="text-2xl text-black font-bold tracking-tighter mb-3">All Blogs</h1>

            <div class="users flex gap-3 flex-wrap">

                <% if (users.length > 0){ %>
                <% users.forEach(function(user){ %>
                <div class="user max-w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-52 rounded-lg bg-zinc-500 overflow-hidden">
                        <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter font-semibold"><%= user.name%></h3>
                    <h5 class="text-zinc-500 mt-1"><%= user.blog%></h5>
                    <div class="flex gap-3 mt-6 justify-between">
                        <a class="bg-cyan-600 text-gray rounded-md p-2 font-bold" href="/edit/<%= user.id%>">Edit Blog</a>
                        <form action="/delete/<%= user.id %>" method="get"
                            onsubmit="return confirm('Are you sure you want to delete this user?');">
                            <button type="submit" class="bg-red-600 text-white font-bold rounded-md p-2">Delete</button>
                        </form>
                    </div>
                </div>
                <% }) %>
                </div>
            </div>
                <% }  else { %>
                       <h3 class="text-zinc-500">No Blog Added</h3>
              <%  } %>


               
    </body>

</html>